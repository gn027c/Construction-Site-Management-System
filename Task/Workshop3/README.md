# PHÂN CÔNG NHIỆM VỤ CHI TIẾT - WORKSHOP 3
> **Mục tiêu Workshop 3**: Hoàn chỉnh kế thừa (Inheritance) và đa hình (Polymorphism) giữa các lớp. Hoàn thành Mục 4 (Implementation of Basic Functions) & Mục 5 (OOP Design & UML).

## Phân công thành viên:
- **Thành viên 1 (Khang - Leader)**: Tối ưu tính đa hình (Dynamic Binding) khi duyệt danh sách `List<Person>` gọi `displayDetails()`; vẽ sơ đồ UML Class Diagram hoàn chỉnh cho Mục 5.5.
- **Thành viên 2 (Tuấn - Nhân sự)**: Hoàn thiện tính bao đóng và kế thừa cho `Worker`, `Contractor`, `Visitor`; viết tài liệu giải thích nguyên lý Encapsulation & Inheritance cho Mục 5.1 & 5.2.
- **Thành viên 3 (Thiên - Zone & Sự cố)**: Hiện thực hóa lớp con `RestrictedZone.java` kế thừa `Zone`, override phương thức `checkAccess(Person p)` kiểm tra chứng chỉ an toàn lao động (`safetyCertId`) và danh sách Whitelist; viết Mục 4.2 & 5.3.
- **Thành viên 4 (Lợi - Data & QA)**: Viết chương trình demo đa hình `ZonePolymorphismDemo.java` chứng minh không cần dùng lệnh `instanceof` thủ công; cập nhật AI Audit Log (Entry 005).
